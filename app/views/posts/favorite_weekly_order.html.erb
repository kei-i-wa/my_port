<p class="text-center" style="color:blue; font-size:15px;"><i class="far fa-smile"></i>現在、「過去１週間のいいね数ベスト10」を表示しています。<p>
<%= render 'posts/search', tag_list: @tag_list,posts: @posts %>

<% unless @posts.present? %>
<p>まだ投稿はありません</p>
<% end %>

<div class="col-md-7 ml-5">
  <% @posts.each do |post|%>
  <div class="card-body">
      <!--タイトル表示される文字数は２５文字までにしてます-->
      <h6 class="card-title">
        <%=link_to post_path(post.id) do %><%= post.title.truncate(25) %>
        <% end %>
      </h6>
      <!--お気に入り、コメント数、投稿者は部分テンプレ-->
        <div id="<%=post.id %>"><%= render 'favorites/favorite', post: post %>
        </div>
  </div>
  <% end %>
</div>

  
</div>
<!--rowのとじタグ-->
</div>
       <div class="pagination pagination-sm justify-content-center">
       <%= paginate @posts %></div>

</div>


<style>
.card{
  padding:0;
  margin:0;
  border:none;
}

.post_box {
    padding: 0.5em 1em;
    /*margin: 2em 0;*/
    color: #474747;
    background: whitesmoke;/*背景色*/
    border-left: double 7px #4ec4d3;/*左線*/
    border-right: double 7px #4ec4d3;/*右線*/
}


h6 {
  /*線の種類（実線） 太さ 色*/
  border-bottom: solid 3px black;
}
</style>